name: CI CANDIDATE TAMPER PROBE

on:
  pull_request:

permissions: {}

jobs:
  sentinel:
    name: Candidate tamper sentinel
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Candidate workflow ran without trusted CI jobs."
