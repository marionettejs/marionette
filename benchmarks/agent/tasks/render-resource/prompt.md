Repair createHost(makeResource), which returns a Marionette View with a working template. Each successful render must acquire exactly one fresh resource by calling makeResource(view); the callback returns {dispose()}. The new resource must remain alive after that render. Before the next resource is acquired, dispose the old one exactly once. Destroy disposes the current resource exactly once; repeated destroy and render after destroy acquire nothing and cause no duplicate disposal. Destroy before any render is safe. Preserve a normal View render lifecycle; do not override or suppress public render/destroy or their events. The current implementation prematurely closes the resource. You may replace its cleanup design.

Use only documented public marionette APIs and native JavaScript. No private framework members, network, timers, global singletons or dependency changes. Implement the entire solution in solution.mjs. Callback resources are externally owned except for your obligation to dispose each acquired resource once; callbacks work synchronously and dispose succeeds. Do not change the supplied task behavior to match one implementation technique. Hidden tests observe lifecycle outcomes, retained identities and external disposal counts.
