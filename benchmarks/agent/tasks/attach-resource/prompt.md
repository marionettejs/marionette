Repair createPanel(el, connect), returning {view, show(), hide(), destroy()}. el is attached to the document. show displays the same live Marionette View through its Region; hide detaches it without destroying it, allowing show again. Each attachment must acquire one connection via connect(view), returning {dispose()}, and keep it alive while attached. hide disposes that connection exactly once. Reattachment creates a fresh connection; repeated show/hide calls do not duplicate acquisition or disposal. Rendering an already-attached View must not replace its connection. Final destroy cleans the Region, View and any active connection; repeated destroy and show after destroy must not acquire again. Destroy while detached is safe. Preserve normal View/Region lifecycle methods/events. The current code closes connections prematurely. You may replace its cleanup design.

Use only documented public marionette APIs and native JavaScript. No private framework members, network, timers, global singletons or dependency changes. Implement the entire solution in solution.mjs. Callback resources are externally owned except for your obligation to dispose each acquired resource once; callbacks work synchronously and dispose succeeds. Do not change the supplied task behavior to match one implementation technique. Hidden tests observe lifecycle outcomes, retained identities and external disposal counts.
