# Diagnostic reference

Stable Marionette diagnostic codes will link to version-neutral pages under
`/errors/<code>/`. Codes are never reused, and a deprecated code keeps a page that
identifies its replacement.

The diagnostic catalog is not yet published. See the [development documentation](/next/)
for current framework behavior.
