{
  "name": "marionette-consumer-lint-fixture",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "node validate.mjs && tsc -p tsconfig.esm.json && tsc -p tsconfig.cjs.json"
  },
  "devDependencies": {
    "eslint": "10.10.0",
    "typescript": "6.0.3"
  }
}
